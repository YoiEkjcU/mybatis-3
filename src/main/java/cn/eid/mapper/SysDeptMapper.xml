<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.eid.mapper.SysDeptMapper">

    <select id="getById" resultType="cn.eid.entity.SysDept">
        select
            dept_id     deptId,
            pid         pid,
            pids        pids,
            simple_name simpleName,
            full_name   fullName,
            description description,
            version     version,
            sort        sort,
            create_time createTime,
            update_time updateTime,
            create_user createUser,
            update_user upeqteUser
        from 
            sys_dept
        where 
            dept_id=#{id}
    </select>
</mapper>
